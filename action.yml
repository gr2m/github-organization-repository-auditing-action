name: Organization Repository Audit
description: "Audit access to all repositories across an organization"
branding:
  icon: "archive"
  color: purple
outputs:
  repositories:
    description: "JSON string for the audited repositories"
runs:
  using: "node12"
  main: "dist/index.js"
